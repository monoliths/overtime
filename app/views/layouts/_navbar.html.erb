<!-- TODO: Works but can use a refactor -->
<nav class="custom-nav navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to 'Overtime', root_path, class: "navbar-brand" %>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                <li class="<%= route_active?(posts_path) %>">
                  <%= link_to 'Time Entries', posts_path %>
                </li>
                <li class="<%= route_active?(new_post_path) %>">
                  <%= link_to 'Add New Entry', new_post_path, id: "new_post_from_nav" %>
                </li>
                <%= render 'shared/audit_log_tab' if policy(AuditLog).index? %>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" data-toggle="dropdown" class="dropdown-toggle">Options<strong class="caret"></strong>
                    </a>
                    <ul class="dropdown-menu">
                        <li><%= link_to 'Admin Dashboard', admin_root_path if User.admin?(current_user) %></li>
                        <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li>
                    </ul>
                </li>
            </ul>
        </div>
        <!--/.nav-collapse -->
    </div>
</nav>
